<Window x:Class="OmegaDSD.ThemeWPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:OmegaDSD.ThemeWPF.ViewModels"
        xmlns:views="clr-namespace:OmegaDSD.ThemeWPF.Views"
        Title="MainWindow"
        Width="800"
        Height="450"
        mc:Ignorable="d">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <views:ThemeManagerUserControl Grid.Column="0" />
        <Border BorderThickness="1" Background="{DynamicResource WindowBackground}" BorderBrush="Gray" Grid.Column="1" Margin="5">
            <ContentControl Margin="10" Content="{Binding SelectedObjectPanel, Mode=OneWay}" />
        </Border>
    </Grid>
</Window>