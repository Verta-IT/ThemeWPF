<UserControl x:Class="OmegaDSD.ThemeWPF.Views.ThemeManagerUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:OmegaDSD.ThemeWPF.Views"
             xmlns:vm="clr-namespace:OmegaDSD.ThemeWPF.ViewModels"
             d:DesignHeight="300"
             d:DesignWidth="250"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary Source="../Themes/LightTheme.xaml" />
    </UserControl.Resources>
    <d:UserControl.DataContext>
        <vm:MainWindowViewModel/>
    </d:UserControl.DataContext>
    <Border Background="{StaticResource WindowBackground}">
        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <GroupBox Grid.Row="0" Header="Select theme">
                <ListBox BorderThickness="0" ItemsSource="{Binding ThemeCollection}" SelectedItem="{Binding SelectedTheme, Mode=TwoWay}"/>
            </GroupBox>
            <GroupBox Grid.Row="1"
                      Margin="0,5,0,0"
                      Header="Controls">
            </GroupBox>
        </Grid>
    </Border>
</UserControl>
